# Database configuration
DB_PATH=./data/crops.db

# Images directory
IMAGES_PATH=./static/images

# Server configuration
PORT=8547
HOST=0.0.0.0

# Site password (leave empty to disable password protection)
# Example: SITE_PASSWORD=mysecurepassword
SITE_PASSWORD=

# API tokens for external access (comma-separated)
# Example: API_TOKENS=token1,token2,token3
API_TOKENS=

# Normalizer paths (optional, for image preprocessing)
# UNFILTERED_DIR=/path/to/unfiltered
# NORMAL_DIR=/path/to/normal
# PYTHON_SCRIPT=/path/to/normalize_image.py
# PYTHON_VENV=/path/to/.venv/bin/python

